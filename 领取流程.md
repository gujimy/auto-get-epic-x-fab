# 领取流程说明

本文档用于人工检查当前脚本的自动领取逻辑是否符合预期。

## 1. 总体结构

当前自动化由 3 层组成：

1. `background.js`
   负责定时调度、打开标签页、并发控制、跨 frame 通信、记录调试日志。
2. `content.js`
   负责在具体页面内识别状态、查找按钮、执行点击、返回当前步骤结果。
3. `common.js`
   负责通用状态解析、Fab 列表页解析、Epic/Fab 公共文案和截止时间解析。

## 2. 后台总流程

入口在 `background.js` 的 `runCheckInternal()`。

执行顺序如下：

1. 读取设置与上次状态。
2. 根据触发来源判断：
   - 自动定时任务
   - 手动检查
   - `只领 Epic`
   - `只领 Fab`
3. 标记 `running=true`，清空本轮调试日志。
4. 处理 Epic：
   - 拉取 Epic 免费游戏接口。
   - 得到当前周免与下周预告。
   - 对当前周免逐个进入自动化流程。
5. 处理 Fab：
   - 先决定是否跳过本轮自动执行。
   - 不跳过时，打开 `https://www.fab.com/limited-time-free` 扫描列表。
   - 得到当前限免条目后逐个进入自动化流程。
6. 写回最终状态，标记 `running=false`。
7. 如有需要，发送浏览器通知。

## 3. “只领 Epic / 只领 Fab” 逻辑

当前规则：

- `只领 Epic`
  只执行 Epic 流程，Fab 直接跳过。
- `只领 Fab`
  只执行 Fab 流程，Epic 直接跳过。
- 普通“检查一次”
  同时执行 Epic 和 Fab。

## 4. Fab 列表页扫描逻辑

入口在 `content.js` 的 `scan-fab-listings` 消息，实际解析函数在 `common.js` 的 `parseFabListingsFromDocument()`。

脚本会：

1. 打开 `https://www.fab.com/limited-time-free`
2. 检查是否出现 Cloudflare 验证
3. 找到列表页标题：
   - 示例：`Limited-Time Free (Until May 5 at 9:59 AM ET)`
4. 解析统一截止时间：
   - 由 `common.js` 的 `parseFabCampaignEndDate()` 完成
   - 写入每个条目的 `endDate`
5. 遍历所有 `/listings/` 链接，提取：
   - `id`
   - `title`
   - `url`
   - `status`
   - `endDate`

Fab 列表状态判断：

- 若卡片内包含 `Saved in My Library / 已保存到我的库 / 在库中`
  记为 `already-owned`
- 若卡片内包含 `Free / -100% / Add to Cart / 添加至购物车`
  记为 `claimable`

## 5. 到点后延迟领取规则

入口在 `background.logic.js` 的 `pickNextAutoRunAt()`。

当前批次全部领取成功后，会把“下次自动执行时间”写入 `executionRecords[site].nextAutoRunAt`。

时间来源：

1. 当前批次的 `endDate`（本轮结束 / 切换点）
2. 预告批次的 `startDate`（下一轮开领点）

额外缓冲：

- 默认在官方时间点后再延迟 `10` 分钟
- 常量：`CLAIM_START_DELAY_MS = 10 * 60 * 1000`
- 原因：平台到点后不一定会实时刷新可领状态

因此：

- 官方开领 `15:00` 时，自动任务实际会等到 `15:10` 才开始
- 手动“检查一次 / 只领 Epic / 只领 Fab”不受该等待限制

## 6. Fab 自动跳过规则

入口在 `background.js` 中的 Fab 列表检测分支，判定函数为
`background.logic.js` 的 `shouldSkipFabDetailInspection()`。

仅对自动任务生效，不影响手动点“只领 Fab”。

跳过条件必须同时满足：

1. 本轮不是手动触发
2. 当前刚抓到的 Fab 列表页存在条目
3. 列表页当前批次全部已显示为：
   - `claimed`
   - 或 `already-owned`
4. 当前批次存在统一 `endDate`
5. 现在时间仍然早于该 `endDate`

满足后：

- 本轮自动任务仍会轻量打开 Fab 列表页做确认
- 但不会再打开每个详情页重复执行领取
- 调试日志中会记录“截止前跳过详情页执行”

## 7. Epic 自动领取流程

入口在 `content.js` 的 `handleEpicStep()`。

每一步会按下面顺序判断：

1. 是否 Cloudflare 页面
   - 是：返回 `challenge-required`
2. 是否已经出现成功文案
   - 是：返回 `claimed`
3. 是否登录页
   - 是：返回 `login-required`
4. `purchase-cta-button` 是否已经是“已在库中”
   - 是：返回 `already-owned`
5. 页面文本状态是否识别为“已在库中”
   - 是：返回 `already-owned`
6. 检查是否存在“获取”按钮
   - 找到则点击，返回 `navigating`
7. 若已进入订单/购买流程：
   - 若订单页仍在加载：返回 `navigating`
   - 若识别到托管结算页：尝试点击“下订单 / Place Order”
   - 若按钮暂未可点：返回 `navigating`，等待下一轮
8. 若仍处于购买流程但未完成：
   - 返回 `navigating`
9. 若页面还显示可领取但未识别出下一步按钮：
   - 返回 `claimable`
10. 其他情况：
   - 返回 `needs-manual`

Epic 关键按钮候选：

- 获取：`获取 / GET`
- 去结算：`去结算 / 前往结算 / CHECKOUT`
- 下订单：`确认订单 / 下单 / PLACE ORDER / 接受 / 同意 / 继续`

Epic 购买流识别条件：

- URL 包含 `/purchase`
- URL 含 `offers=`
- URL 含 `showNavigation=true`
- 页面内出现：
  - `#purchase-app`
  - `#purchaseAppContainer`
  - `.payment-confirm-container`
  - `.payment-order-confirm`

## 8. Fab 自动领取流程

入口在 `content.js` 的 `handleFabStep()`。

每一步会按下面顺序判断：

1. 是否 Cloudflare 页面
   - 是：返回 `challenge-required`
2. 是否出现 Fab 成功弹层
   - 是：返回 `claimed`
3. 是否出现通用订单成功文案
   - 是：返回 `claimed`
4. 是否登录授权页
   - 是：返回 `login-required`
5. 页面文本状态是否识别为已在库中
   - 是：返回 `already-owned`
6. 若页面存在“在库中 / 我的库 / 下载”按钮
   - 返回 `already-owned`
7. 进入领取模式后：
   - 若订单页仍在加载：返回 `navigating`
   - 若已进入托管结算页：尝试点击 `Place Order`
   - 若页面有 `立即购买 / Add to Cart`：优先点击
   - 若页面有结算/确认订单按钮：继续点击
   - 若仍处于购买流程：返回 `navigating`
8. 若页面还显示可领取但未识别到下一步：
   - 返回 `claimable`
9. 其他情况：
   - 返回 `needs-manual`

Fab 购买流识别条件：

- URL 含 `purchaseToken`
- URL 含 `quickBuyOfferId`
- URL 路径包含 `/payment/web/purchase`
- 页面内出现：
  - `iframe[src*="/payment/web/purchase"]`
  - `#purchase-app`
  - `#purchaseAppContainer`
  - `.payment-confirm-container`
  - `.payment-order-confirm`

## 9. 托管结算页按钮点击逻辑

Epic 和 Fab 共用一套托管支付页点击逻辑。

关键函数在 `content.js`：

- `clickHostedPaymentConfirmButton()`
- `waitForActionableSelector()`
- `clickElementRobust()`
- `dispatchPointerSequence()`

执行顺序：

1. 先用已知 CSS 选择器直接找按钮
2. 如果没找到，最多等待 8 秒轮询按钮出现
3. 找到后执行：
   - `scrollIntoView`
   - `focus`
   - 指针事件序列
   - 原生 `click()`

托管结算页按钮选择器：

- `button.payment-order-confirm__btn`
- `.payment-order-confirm button.payment-btn--primary`
- `.payment-confirm-container button.payment-btn--primary`
- `#purchase-app .payment-order-confirm__btn`
- `#purchase-app button.payment-btn--primary`

## 10. 后台自动化步进机制

入口在 `background.js` 的 `driveAutomation()`。

每个条目最多循环 `20` 步。

每一步执行：

1. 等待标签页加载一小段时间
2. 如果仍在 `loading`，直接继续和页面通信，不再硬等 `complete`
3. 向页面发送 `automation-step`
4. 根据页面返回值决定：
   - `navigating`
     说明本轮点击成功或仍在等待，延迟后进入下一步
   - 其他状态
     作为最终结果返回

## 11. 多窗口并发策略

当前在 `background.js` 中使用 `mapWithConcurrency()`。

并发数：

- `OFFER_CONCURRENCY = 3`

意味着：

- Epic/Fab 条目会最多同时开 3 个后台标签页处理
- 保留原始列表顺序写回状态

## 12. 跨 frame 通信逻辑

因为 Epic/Fab 的购买页可能在子 frame 中渲染，所以后台并不是只给顶层页面发消息。

逻辑在 `background.js`：

- `sendMessageWithRetry()`
- `sendMessageToCandidateFrames()`
- `getCandidateFrameIds()`
- `pickBestFrameResponse()`

规则：

1. 自动化步骤默认扫描全部 frame
2. Fab 列表页扫描只看顶层 frame
3. 若多个 frame 都返回结果，按优先级挑选：
   - `claimed`
   - `already-owned`
   - `navigating`
   - `challenge-required`
   - `login-required`
   - `claimable`
   - `needs-manual`
   - `error`
   - `unknown`

## 13. 调试日志来源

调试日志由 `background.js` 的 `appendDebugLog()` 持续写入侧边栏状态。

主要记录：

- 任务开始与结束
- 当前免费数量
- 页面打开/关闭
- 每一步自动化返回值
- 是否仍在 loading
- 是否跳过了某个站点
- Fab 是否因未过期而跳过自动执行

单个条目详情里的“调试日志（N步）”来自 `driveAutomation()` 内部的 `trace`。

## 14. 当前人工检查重点

人工检查时建议优先关注以下几点：

1. Epic：
   - 点“获取”后是否真正进入 `purchase` 流程
   - `下订单` 是否被识别为托管结算页按钮
2. Fab：
   - `立即购买` 后是否切到 `purchaseToken` 流
   - `Place Order` 是否出现在子 frame
3. 列表状态：
   - Fab 列表页是否正确识别已在库中
   - 截止时间是否正确显示
4. 跳过策略：
   - 自动任务是否在 Fab 全部已领且未过期时跳过
   - 手动“只领 Fab”是否仍然强制执行
5. 侧边栏：
   - 调试日志是否持续刷新
   - 滚动位置与折叠状态是否保持

## 15. 当前已知行为结论

基于当前脚本，自动领取逻辑的目标行为是：

- Epic：
  点击“获取”后，自动进入购买页并点击“下订单”，直到成功或进入人工确认。
- Fab：
  在列表页先区分已在库中，未领取的才打开详情页；进入购买流后自动点击 `Place Order`。
- 自动任务：
  在不需要重复检查的情况下尽量跳过 Fab，减少无意义执行；
  到点后会再等 10 分钟再开始领取，给平台刷新留缓冲。
- 手动任务：
  始终优先执行用户当前要求，不受自动跳过与到点延迟规则影响。
